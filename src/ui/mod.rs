//! The eframe app (feature `gui`). Built in a later phase; see docs/STATUS.md.

/// Open `path` (default `.`) in the running app, or start the app. Returns the exit code.
pub fn launch(path: Option<String>) -> i32 {
    let _ = path;
    todo!()
}
